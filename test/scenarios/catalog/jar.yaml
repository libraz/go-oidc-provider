# yaml-language-server: $schema=./schema.json
feature: jar
prefix: JAR
title: JAR — JWT-Secured Authorization Request
specs:
  - RFC 9101 — The OAuth 2.0 Authorization Framework — JWT-Secured Authorization Request (JAR)
  - OpenID Connect Core 1.0 §6 (Passing Request Parameters as JWTs)
  - OpenID Connect Discovery 1.0 §3 (request_parameter_supported,
    request_object_signing_alg_values_supported,
    require_signed_request_object)
  - RFC 8628 §3.1 — Device Authorization Endpoint

description: |
  JWT-Secured Authorization Request (Request Object) handling at the
  authorization and device authorization endpoints. Covers discovery
  surface, parameter precedence and coercion, signing-alg policy,
  inception and registration restrictions, error envelope shape, and
  state preservation across error paths.

rows:
  - id: JAR-001
    severity: P1
    spec: RFC 9101 §10.5 / OIDC Discovery §3
    behaviour: |
      When JAR is enabled without require_signed_request_object,
      /.well-known/openid-configuration advertises
      request_parameter_supported=true and does NOT include
      require_signed_request_object.
    status: active

  - id: JAR-002
    severity: P1
    spec: RFC 9101 §10.5
    behaviour: |
      When require_signed_request_object is toggled on, the discovery
      document advertises require_signed_request_object=true so RPs
      cannot rely on plaintext requests.
    status: out-of-scope
    out_of_scope_reason: v1.0 discovery builder never assigns Document.RequireSignedRequestObject; the FAPI 2.0 Message Signing profile drives the runtime gate but does not surface as the require_signed_request_object metadata field.

  - id: JAR-003
    severity: P0
    spec: RFC 9101 §5 / OIDC Core §6.1
    behaviour: |
      Request Object claims override outer query/form parameters at
      both the authorization and device authorization endpoints; the
      OP honours only the Request Object values.
    status: active

  - id: JAR-004
    severity: P2
    spec: OIDC Core §6.1
    behaviour: |
      Numeric Request Object claims (e.g. max_age) MUST be coerced to
      strings before downstream processing so all authorization
      parameters share a single representation.
    status: active

  - id: JAR-005
    severity: P1
    spec: RFC 6749 §3.1.2 / OIDC Core §6.1
    behaviour: |
      A Request Object that supplies scope as a JSON array (forcing
      duplicate semantics) is rejected with invalid_request and a
      message indicating scope cannot be supplied twice.
    status: out-of-scope
    out_of_scope_reason: "v1.0's stringifyClaim space-joins []string for shared keys (scope / acr_values / ui_locales); the catalog's duplicate-semantics rejection has no v1.0 gate."

  - id: JAR-006
    severity: P2
    spec: OIDC Core §5.5 / §6.1
    behaviour: |
      A claims parameter delivered as a serialised JSON string in the
      Request Object is accepted and exposed verbatim to downstream
      params.
    status: out-of-scope
    out_of_scope_reason: "v1.0's JAR merger treats 'claims' as a JSON-encoded shape (mergeJSONClaims) and json.Marshal-wraps any value, so a string-valued claims claim becomes a JSON-string-of-a-string on the wire and the authorize parser rejects it as not-an-object. The catalog's verbatim-passthrough variant has no v1.0 path; JAR-007 covers the documented JSON-object form."

  - id: JAR-007
    severity: P2
    spec: OIDC Core §5.5 / §6.1
    behaviour: |
      A claims parameter delivered as a JSON object in the Request
      Object is accepted and re-serialised back to a canonical JSON
      string.
    status: active

  - id: JAR-008
    severity: P2
    spec: RFC 9101 §10.8 / RFC 7519 §4.1.6
    behaviour: |
      A Request Object whose iat is slightly in the future but within
      the configured clock-skew tolerance is accepted.
    status: active

  - id: JAR-009
    severity: P1
    spec: RFC 9101 §6.1
    behaviour: |
      A Request Object signed with HS256 by a client whose
      request_object_signing_alg=HS256 is accepted.
    status: out-of-scope
    out_of_scope_reason: "v1.0 rejects HMAC algorithms (HS256/HS384/HS512) at parse time via the project-wide JOSE allow-list; an HS256-signed request object never reaches the per-client alg-pin gate, so the catalog's HS256-accepted variant has no reachable v1.0 path. Mirrors JAR-010."

  - id: JAR-010
    severity: P0
    spec: RFC 9101 §6.1 / OIDC Core §6.3
    behaviour: |
      A Request Object signed with an HMAC key whose backing client
      secret has expired is rejected with invalid_request_object
      indicating an expired secret.
    status: out-of-scope
    out_of_scope_reason: v1.0 rejects HMAC algorithms (HS256/HS384/HS512) at parse time via internal/jose Algorithm.IsAllowed; the expired-secret sub-state cannot be reached because no HMAC-signed request object reaches the verifier.

  - id: JAR-011
    severity: P0
    spec: RFC 9101 §10.4 / OIDC Core §6.1
    behaviour: |
      A Request Object that nests a request parameter is rejected
      with invalid_request_object; Request Objects MUST NOT contain
      request or request_uri.
    status: active

  - id: JAR-012
    severity: P0
    spec: RFC 9101 §10.4
    behaviour: |
      A Request Object that nests a request_uri parameter is rejected
      with invalid_request_object.
    status: active

  - id: JAR-013
    severity: P1
    spec: OIDC Core §6.1
    behaviour: |
      When the Request Object specifies response_mode=fragment at the
      authorization endpoint, the OP applies fragment encoding for
      the response.
    status: out-of-scope
    out_of_scope_reason: "v1.0's authorize validator admits only {query, form_post, query.jwt, fragment.jwt, form_post.jwt, jwt}; bare 'fragment' is not on the allow-list because v1.0 ships response_type=code only. JAR-014 already covers the unsupported-mode rejection path."

  - id: JAR-014
    severity: P1
    spec: RFC 6749 §3.1.2 / OIDC Core §6.1
    behaviour: |
      A Request Object that carries an unsupported response_mode at
      the authorization endpoint is rejected with
      unsupported_response_mode.
    status: active

  - id: JAR-015
    severity: P0
    spec: RFC 9101 §5 / OIDC Core §6.1
    behaviour: |
      When the outer response_type differs from the Request Object
      claim at the authorization endpoint, the request is rejected
      with invalid_request_object (response_type mismatch).
    status: out-of-scope
    out_of_scope_reason: "v1.0's RFC 9101 §6.1 contract is silent JWT-over-outer override (internal/jar Merge); there is no response_type-mismatch gate. PAR-021 already exercises override-wins precedence."

  - id: JAR-016
    severity: P1
    spec: RFC 6749 §4.1.2.1 / RFC 9101 §10.7
    behaviour: |
      When a client_id mismatch error fires and the Request Object
      carried a state value, the authorization error response carries
      that state so the RP can correlate the failure.
    status: out-of-scope
    out_of_scope_reason: "JAR verifier failures (including client_id mismatch) render inline via writeJAREnvelopeError -> renderJARError -> renderBrowserError: an interaction.Driver-rendered page for a caller that prefers text/html, the JSON envelope otherwise. Neither shape is a redirect, so no error response on this path carries state back to the RP. The state renderBrowserError hands the driver is the OUTER wire value read before verification (internal/authorizeendpoint/jar_consumption.go resolveJARRequestIfNeeded), so a state carried only inside the Request Object is never surfaced at all — the object's claims are never merged once Verify fails."

  - id: JAR-017
    severity: P0
    spec: RFC 9101 §5
    behaviour: |
      When the outer client_id differs from the Request Object
      client_id at either the authorization or device authorization
      endpoint, the request is rejected with invalid_request_object
      (client_id mismatch).
    status: out-of-scope
    out_of_scope_reason: "v1.0 emits invalid_request (not invalid_request_object) for wire-vs-JWT client_id mismatch (writeJAREnvelopeError treats it as a wire consistency violation); the /authorize side shares the gate with PAR-023. The catalog's device_authorization_endpoint half has no v1.0 surface (there is no device-flow feature flag)."

  - id: JAR-018
    severity: P0
    spec: RFC 9101 §6.1 / RFC 7519 §7.2
    behaviour: |
      An outer request parameter that is not a JWT (e.g.
      "definitely.notsigned.jwt") is rejected with
      invalid_request_object and a parse-failure description.
    status: active

  - id: JAR-019
    severity: P0
    spec: RFC 9101 §6.1 / OIDC Core §6.3
    behaviour: |
      A Request Object signed with an alg different from the client's
      pre-registered request_object_signing_alg is rejected with
      invalid_request_object (the pre-registered alg MUST be used).
    status: active

  - id: JAR-020
    severity: P0
    spec: RFC 9101 §10.1
    behaviour: |
      A Request Object signed with an alg outside the server's
      configured supported list is rejected with
      invalid_request_object (unsupported signed request alg).
    status: active

  - id: JAR-021
    severity: P0
    spec: RFC 9101 §6.1 / RFC 7515 §5.2
    behaviour: |
      A Request Object signed with the wrong key/secret is rejected
      with invalid_request_object and a validation-failure
      description.
    status: active

  - id: JAR-022
    severity: P1
    spec: OIDC Core §7.2.1
    behaviour: |
      A Request Object that carries a registration claim is rejected
      with registration_not_supported.
    status: out-of-scope
    out_of_scope_reason: "v1.0 has no registration_not_supported gate; the merger projects 'registration' onto the wire form and the authorize parser silently drops unknown keys (covered by JAR-023)."

  - id: JAR-023
    severity: P2
    spec: RFC 9101 §6.1 / OIDC Core §6.1
    behaviour: |
      A Request Object that includes unrecognised members is accepted
      and the unknown members are ignored per spec.
    status: active
